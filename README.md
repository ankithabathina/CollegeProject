# CollegeProject
I developed a Spring Boot REST API for managing student data. The application follows a layered architecture with controller, service, and repository layers. I used DTO to separate API and database layers, implemented validation to ensure data integrity, and handled exceptions globally using @ControllerAdvice. 
